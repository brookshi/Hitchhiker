import React from 'react';

interface IDraggableTreeProps { }

interface IDraggableTreeState { }

class DraggableTree extends React.Component<IDraggableTreeProps, IDraggableTreeState> {
    public render() {
        return (
            <span>Body</span>
        );
    }
}

export default DraggableTree;

